<div class="container">
  <div class="page-title">
    <div>.disc</div>
    <div>over</div>
  </div>
  <div class="card">
    <div class="row">
      <% @songs.each do |song| %>
        <%= render 'card', song: song%>
      <% end %>
    </div>
  </div>
</div>
